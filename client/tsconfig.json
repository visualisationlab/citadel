{
    "extends": "../tsconfig.json",
    "include": [
        "./src",
    ],
    "compilerOptions": {
        // Emit
        "declarationDir": "./types",
        "outDir": "dist",

        // Language and Environment
        "jsx": "react-jsx",

        // Modules
        "paths": {
            "shared": ["../shared/types/src/types"]
        },
    },
    "eslintConfig": {
        "extends": ["react-app", "shared-config"]
    },
}
