{
    "extends": "../tsconfig.json",
    "include": [
        "./src",
    ],
    "compilerOptions": {
        // Emit
        "declarationDir": "./types",
        "outDir": "dist",

        // Language and Environment
        "jsx": "react-jsx",
    },
    "eslintConfig": {
        "extends": ["react-app", "shared-config"]
    },
}
