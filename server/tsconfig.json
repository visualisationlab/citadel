{
    "extends": "../tsconfig.json",
    "include": [
        "src/**/*"
    ],
    "compilerOptions": {
        // Emit
        "declarationDir": "./types",
        "outDir": "dist",

        // Modules
        "paths": {
            "shared": ["../shared/dist/src/types"]
        },
    },
    "references": [
        {
            "path": "../shared"
        }
    ],
}
